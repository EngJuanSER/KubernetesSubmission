apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app
  namespace: project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
        - name: shared-image
          emptyDir: {}
      containers:
        - name: todo-app
          image: engjuanser/todo-app:2.2
          env:
            - name: PORT
              value: "3000"
          volumeMounts:
            - name: shared-image
              mountPath: /usr/src/app/files
        - name: image-fetcher
          image: engjuanser/image-fetcher:1.12
          volumeMounts:
            - name: shared-image
              mountPath: /usr/src/app/files
